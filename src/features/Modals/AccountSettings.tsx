/**
 * This modal should be so the user can change the following defaults under their account:
 * 1. Email
 * 2. Password
 * 3. Default theme
 * 4. Username
 * 
 */
import React from 'react';

const AccountSettings = ()=> {
    return (
        <>account settings</>
    )
}
export default AccountSettings;